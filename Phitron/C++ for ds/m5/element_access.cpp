#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define nl "\n"

#define faster ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);

int32_t main()
{
    faster;
    string s = "hello world";
    cout<<s[10]<<endl;// access the ith index of the string.

    cout<<s.at(9)<<endl;// access the ith index of the string.

    cout<<s.back()<<endl;// access the last element of the string.

    cout<<s.front()<<endl;// access the first element of the string.

    return 0;
}