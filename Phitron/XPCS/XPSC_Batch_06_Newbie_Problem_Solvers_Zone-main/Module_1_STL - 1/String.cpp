#include <bits/stdc++.h>
using namespace std;

int main() {
   ios::sync_with_stdio(false);
   cin.tie(nullptr);

   // int n;
   // cin >> n;
   // string s;
   // cin >> s;
   // cout << s << '\n';
   // for (int i = 1;i <= n;i++) {
   //    char c;
   //    cin >> c;
   //    s.push_back(c);
   // }
   // s.pop_back();
   // cout << s << '\n';
   // cout << s.front() << " " << s.back() << '\n';
   // s.clear();
   // cout << s.empty() << '\n';


   // string s;
   // cin >> s;
   // cout << s.substr(1, 3) << '\n';
   // cout << s.substr(2) << '\n';

   return 0;
}