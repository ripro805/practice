#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define nl "\n"

#define faster ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);

int32_t main()
{
    faster;
    int t,s;
    cin >> t>>s;
    for(int i=1; i<=t; i++)
       {
        for(int j=0;j<s;j++){
            cout<<"Hello"<<endl;
       }
       }
    return 0;
}
//here we get final complexity O(n*m)....for nested loop time complexitty will be multiplied