#include<stdio.h>
int main()
{
    float width,length,area;
    printf("Enter the width:");
    scanf("%f",&width);
    printf("Enter the length:");
    scanf("%f",&length);
    area=width*length;
    printf("The area is=%f\n",area);
}

