#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define nl "\n"

#define faster ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);

int32_t main()
{
    long long x;
    while (true){
        cin>>x;
        if(x==1999){
          cout<<"Correct"<<endl;
          break;  
        }
        else{
            cout<<"Wrong"<<endl;
        }
    }

    return 0;
}
