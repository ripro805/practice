#include <bits/stdc++.h>
#include <algorithm>
using namespace std;
typedef long long ll;
#define nl "\n"

#define faster ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);

int32_t main()
{
    int n;
    cin>>n;
    string s;
    cin>>s;

   
    sort(s.begin(),s.end());
    cout<<s<<endl;

    return 0;
}